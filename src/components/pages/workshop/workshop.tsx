import React from 'react';
import PageHeader from '../../multiPageComponents/pageHeader';

export default function Workshop():React.ReactElement {

    return (
        <React.Fragment>
            <PageHeader heading="Maths Workshop" subheading="A place to ask questions and get help with your maths" />
        </React.Fragment>
    );
};